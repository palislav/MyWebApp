var counter = function(arr){
    return 'Je/su tam ' + arr.length + ' elementov/elementy'
}

var adder = function(a, b){
    return `sucet cisiel je ${a+b}`;
}

var pi = 3.142;

module.exports = {
    counter: counter,
    adder: adder,
    pi: pi,
};